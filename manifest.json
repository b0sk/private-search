{
  "manifest_version": 2,
  "name": "Private Search",
  "version": "1.0",
  "permissions": ["contextMenus", "search"],
  "description": "Adds a menu option to search selected text in a Private Window.",

  "background": {
      "scripts": ["private_search.js"]
  }
}
